import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Typography variant="h4">Welcome to Role Based App</Typography>
      <Typography>Use the header to login or register.</Typography>
    </Container>
  );
};

export default Home;
